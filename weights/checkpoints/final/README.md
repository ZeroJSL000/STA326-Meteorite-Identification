# Final Checkpoints

Place the following five files in this directory:

- `fold_0_best.pth`
- `fold_1_best.pth`
- `fold_2_best.pth`
- `fold_3_best.pth`
- `fold_4_best.pth`

Each checkpoint is about 332 MiB and exceeds GitHub's 100 MiB regular Git limit.
Run `uv run python scripts/verify_assets.py` to verify the layout.
